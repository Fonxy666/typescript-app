import React, { FormEvent, useRef, useState } from 'react';

const HomePage: React.FC = () => {
   

    return (
        <div>
            Hello World!
        </div>
    );
};

export default HomePage;